{
  "name": "maki-kiosk",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build && node scripts/stamp-sw.mjs dist",
    "deploy": "npm run build && node scripts/deploy.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "shots": "vite build && (vite preview --port 4173 --strictPort & sleep 3 && node shots.mjs shots)",
    "shots:real": "node realshots.mjs",
    "flow": "node flowcheck.mjs",
    "wx": "node wxagree.mjs",
    "wx:shots": "vite build && node wxshot.mjs",
    "wx:verify": "node wxverify.mjs",
    "savefail": "node savefail.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "playwright": "^1.49.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
